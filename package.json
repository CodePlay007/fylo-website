{
  "name": "simple-tailwind-starter",
  "version": "1.0.0",
  "description": "Simple tailwindcss starter boilerplate configuration for small projects.",
  "main": "index.js",
  "scripts": {
    "watch": "tailwindcss -i ./input.css -o ./css/style.css --watch",
    "build": "tailwindcss -i ./input.css -o ./css/style.css",
    "devserver": "live-server",
    "start": "npm-run-all --parallel watch devserver"
  },
  "author": "Vibhu Bajaj",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.2",
    "npm-run-all": "^4.1.5",
    "tailwindcss": "^3.2.7"
  }
}
